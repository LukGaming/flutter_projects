import 'package:chat_app/theme/sizes.dart';
import 'package:flutter/material.dart';

const notSentMessageIcon = Icon(
  Icons.access_time,
  size: defaultMessageIconStatusSize,
);

const sentMessageIcon = Icon(
  Icons.done,
  size: defaultMessageIconStatusSize,
);

const receivedMessageIcon = Icon(
  Icons.done_all,
  size: defaultMessageIconStatusSize,
);

const seenMessageIcon = Icon(
  Icons.done_all,
  size: defaultMessageIconStatusSize,
  color: Colors.blueAccent,
);
